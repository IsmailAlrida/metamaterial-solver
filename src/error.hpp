#pragma once

#include <iostream>

namespace App {

// I dont know what this guy is doing
// But he should at least safely catch
// Exceptions and log them to the renderer
// How does that sound? idk. lemme think
class ErrorHandler{

    public:

        ErrorHandler();
        ~ErrorHandler();

        
};

} // namespace App
