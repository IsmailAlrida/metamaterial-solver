#pragma once
#include "global_types.hpp"

class Exporter {
    
    public:
        Exporter();
        // TODO: Change the data
        bool export(LevelSet geometry, std::string target);

    private:

}